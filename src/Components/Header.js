import '../App.css';
import logo from './librerias-gandhi.png'

const Header = () => {
    return (  
      <header className="App-header">
        <img src={logo} className="App-logo" alt="Foto" />
      </header>
    );
}
 
export default Header;